---
description: Implement the NEXT item in the Pitch402 build order, then stop
---

Read CLAUDE.md and the pitch402 skill.

1. Inspect the repo and determine which items in the "Build order" section of CLAUDE.md are already done.
2. Pick the FIRST item that is not done. Implement ONLY that item.
3. Follow every rule in the "Do not" section. If the item involves Solidity, delegate to the solidity-dev agent.
4. Verify it works (run it, test it, or `forge test` for contracts).
5. Stop. Report what you built, how you verified it, and what the next item is. Do not start the next item.

$ARGUMENTS
